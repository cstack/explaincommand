{
  "description": "Export a container's filesystem as a tar archive",
  "flags": [
    {
      "aliases": [
        "-o",
        "--output"
      ],
      "description": "  -o, --output string   Write to a file, instead of STDOUT",
      "takes_argument": true
    }
  ],
  "positional_arguments": [

  ]
}